name: Build DomainWatch APK
on:
  push:
    branches: [ "domainwatch-build" ]
jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: domainwatch
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'
      - run: gradle :app:assembleDebug --no-daemon
      - uses: actions/upload-artifact@v4
        with:
          name: domain-nobetcisi-apk
          path: domainwatch/app/build/outputs/apk/debug/app-debug.apk
